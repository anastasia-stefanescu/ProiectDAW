@page
@model IndexModel
@{
    ViewData["Title"] = "Profile";
    ViewData["ActivePage"] = ManageNavPages.Index;
}

<h3>@ViewData["Title"]</h3>
<partial name="_StatusMessage" for="StatusMessage" />
<div class="row">
    <div class="col-md-6">
        <form id="profile-form" method="post">

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @*modificari*@
            <div class="form-group">
                <label asp-for="Input.NumeComplet"></label>
                <input asp-for="Input.NumeComplet" class="form-control" />
                <span asp-validation-for="Input.NumeComplet" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.Rol"></label>
                <input asp-for="Input.Rol" class="form-control" />
                <span asp-validation-for="Input.Rol" class="text-danger"></span>
            </div>

            @*end*@
            <br />
            <div class="form-floating">
                <input asp-for="Username" class="form-control" disabled />
                <label asp-for="Username" class="form-label"></label>
            </div>
            <br />
            <div class="form-floating">
                <input asp-for="Input.PhoneNumber" class="form-control" />
                <label asp-for="Input.PhoneNumber" class="form-label"></label>
                <span asp-validation-for="Input.PhoneNumber" class="text-danger"></span>
            </div>

            @*modificari*@
            <div class="col-md-6">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Input.PozaProfil" style="width: 100%;"></label>
                    @if (Model.Input.PozaProfil != null)
                    {
                        <img id="imgPicture" style="width:350px;height:350px; object-fit:cover" src="data:image/*;base64,@(Convert.ToBase64String(Model.Input.PozaProfil))">
                    }
                    else
                    {
                        <img id="imgPicture" style="width:350px;height:350px; object-fit:cover" src="">
                    }
                    <input type="file"
                           accept=".png,.jpg,.jpeg,.gif,.tif"
                           asp-for="Input.PozaProfil"
                           class="form-control"
                           style="border:0px!important;padding: 0px;padding-top: 10px;padding-bottom: 30px;"
                           onchange="document.getElementById('imgPicture').src = window.URL.createObjectURL(this.files[0])" />
                    <span asp-validation-for="Input.PozaProfil" class="text-danger"></span>
                </div>
            </div>
            @*end modif*@
            <button id="update-profile-button" type="submit" class="w-100 btn btn-lg btn-primary">Save</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
