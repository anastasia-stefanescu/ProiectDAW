@model Proiect.Models.ApplicationUser

<div class="col-4 mb-4">
    <div class="card" style="height:100%; width: 100%;">

        <div class="card-body d-flex justify-content-between flex-column">
            <div>
                <p><i class="bi bi-person"> </i>Nume utilizator: @Model.NumeComplet</p>
                <p><i class="bi bi-person"> </i>Rol utilizator: @Model.Rol</p>
                <p><i class="bi bi-person"> </i>Email utilizator: @Model.Email</p>
            </div>
            
        </div>

    </div>
</div>